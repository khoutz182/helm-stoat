apiVersion: apps/v1
kind: Deployment
metadata:
  name: "{{ include "stoat.fullname" . }}-autumn"
  namespace: {{ .Values.global.namespace }}
  labels:
    {{- include "stoat.labels" . | nindent 4 }}
    stoat-app: autumn
  {{- with (merge (.Values.global.all.deployment.annotations | default dict) (.Values.autumn.annotations | default dict)) }}
  annotations:
    {{- toYaml . | nindent 8 }}
  {{- end }}
spec:
  replicas: {{ .Values.autumn.replicaCount }}
  selector:
    matchLabels:
      {{- include "stoat.selectorLabels" . | nindent 6 }}
      stoat-app: autumn
  template:
    metadata:
      labels:
        stoat-app: autumn
        {{- include "stoat.labels" . | nindent 8 }}
        {{- with .Values.autumn.labels }}
        {{- toYaml . | nindent 8 }}
        {{- end }}
    spec:
      containers:
        - name: autumn
          image: "{{ .Values.autumn.image.repository }}:{{ .Values.autumn.image.tag }}"
          imagePullPolicy: {{ .Values.autumn.image.pullPolicy }}
          ports:
            - name: http
              containerPort: 14704
              protocol: TCP
          livenessProbe:
            {{- if .Values.autumn.livenessProbe }}
            {{- toYaml .Values.autumn.livenessProbe | nindent 12 }}
            {{- else }}
            tcpSocket:
              port: {{ .Values.global.autumn.port }}
            initialDelaySeconds: 15
            periodSeconds: 30
            {{- end }}
          readinessProbe:
            {{- if .Values.autumn.readinessProbe }}
            {{- toYaml .Values.autumn.readinessProbe | nindent 12 }}
            {{- else }}
            tcpSocket:
              port: {{ .Values.global.autumn.port }}
            initialDelaySeconds: 15
            periodSeconds: 30
            {{- end }}
          {{- with .Values.autumn.resources }}
          resources:
            {{- toYaml . | nindent 12 }}
          {{- end }}
          volumeMounts:
            - name: config
              subPath: Revolt.toml
              mountPath: {{ .Values.autumn.configMountPath }}
            {{- with .Values.autumn.extra_volumeMounts }}
            {{ toYaml . | nindent 12 }}
            {{- end }}
      volumes:
        - name: config
          secret:
            secretName: {{ include "stoat.fullname" . }}-config
        {{- with .Values.autumn.extra_volumes }}
        {{ toYaml . | nindent 8 }}
        {{- end }}
      {{- with .Values.autumn.nodeSelector }}
      nodeSelector:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      {{- with .Values.autumn.affinity }}
      affinity:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      {{- with .Values.autumn.tolerations }}
      tolerations:
        {{- toYaml . | nindent 8 }}
      {{- end }}
